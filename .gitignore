# Large data
data/3_data_parquet_relabeled/time_differences_details.pkl
data/4_data_parquet_modeling/all_locations.parquet

# Jupyter
.ipynb_checkpoints/

# Python bytecode
__pycache__/
tools/__pycache__/

# R session history (not needed for reproducibility)
.Rhistory
.Rapp.history

# RData files (can be large and not needed)
.RData
.Ruserdata
.RDataTmp*

# RStudio project settings (optional, but keeps repo clean)
.Rproj.user/

# renv library (locally installed packages)
renv/library/
renv/staging/
renv/activate.R
renv/settings.json
renv/.gitignore
renv.zip

# API Tokens
weather_token.txt

src/foodcast.egg-info/

# renv project library (restore from renv.lock instead)
renv/library/
renv/staging/

# stage-3 handoff to the AI labeling repo: regenerated by 4.1_joining_customers,
# consumed only by automating-labeling, never read back here
data/3_data_parquet_relabeled/3_combined_no_prelabeled_drinks/
run_logs/
