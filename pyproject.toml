[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "restaurant-sales"
version = "0.1.0"
description = "A Python project for analyzing and managing restaurant sales."
readme = "README.md"
authors = [
    { name = "Jared Winslow"},
]
requires-python = ">=3.9"